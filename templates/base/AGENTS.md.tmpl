# Agent Instructions

## Project Summary

`{{projectName}}` is a {{projectKindLabel}} using repository-local AI-agent instructions generated by `codex-agent-template`.

## Source Of Truth

- This `AGENTS.md` file is the primary instruction source for Codex.
- Keep this file short, concrete, and under roughly 150-200 lines.
- Move long procedures to `docs/`, repeatable workflows to skills, and deterministic checks to scripts.

## Workflow

- Start with bounded discovery before editing.
- State assumptions when local evidence is incomplete.
- For meaningful code, infrastructure, content, rules, assets, dependency, or behavior changes, plan first and wait for explicit approval.
- Keep scope narrow and do not include unrelated refactors.
- Do not overwrite existing user changes or generated files without explicit approval.

## Repository Hygiene

- Check Git status before editing when the repository uses Git.
- Do not create commits automatically.
- Do not add hidden dependencies, services, or runtime requirements.
- Keep local-only notes, secrets, and machine-specific settings out of version control.

## Verification

- Use the smallest verification command that proves the change.
- Record checks that are unavailable or not configured.
- Do not claim verification that was not run.

## Generated Metadata

- Agent mode: `{{agent}}`
- Workflow: `{{workflow}}`
- Project kind: `{{projectKind}}`
- Generated: `{{generatedAt}}`
